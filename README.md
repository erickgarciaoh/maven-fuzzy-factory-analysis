# maven-fuzzy-factory-analysis

<!-- Descripción de una línea: qué análisis y qué habilidades demuestra. -->

**[Ver el data-story en vivo](#)** · <!-- link de GitHub Pages cuando esté desplegado -->

## Qué hay aquí
- Pipeline SQL-céntrico (raw → procesado → análisis) en `src/`.
- Data-story interactivo en `docs/` (HTML + ECharts).
- Reporte de Power BI (modelo + DAX) en `powerbi/`. Capturas en `powerbi/captures/`.

## Metodología
<!-- 2-3 líneas: enfoque por fases + loops agénticos contra SQL. Link a una captura o al data-story. -->

## Cómo reproducir
1. Cargar el/los dataset(s) de `data/raw/` con el SP importador (`src/`).
2. Ejecutar los scripts `src/` en orden numérico.
3. Abrir `docs/index.html` (o el sitio de Pages) para el data-story.
